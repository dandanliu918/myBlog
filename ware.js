/** * 编写一个中间件函数 * 当客户端未登陆时候，则跳到登录页，如果已登录则继续访问 * @param req * @param res * @param next */exports.checkMustLogin=function (req,res,next) {    if(req.session.user){        next()    }else {        res.redirect('/user/signin');    }}exports.checkNotLogin=function (req,res,next) {    if(req.session.user){        req.flash('error','你已经登陆请不要重复登陆和注册');        res.redirect('/user/signin');    }else {        req.flash('seccess','你已经登陆成功');        next()    }}