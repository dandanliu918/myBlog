var mongoose=require('mongoose');mongoose.Promise=Promise;var ObjectId=mongoose.Schema.Types.ObjectId;mongoose.connect('mongodb://127.0.0.1/201614blog');var UserSchema=new mongoose.Schema({    username: {type: String, isRequired: true},//必填    password: {type: String, isRequired: true},    email: String,    avatar:String//如果不传 会被忽略})//定义导出model exports.User=mongoose.model('User',UserSchema); var ArticaleSchema=new mongoose.Schema({    title:String,    content:String,    // 类型是日期，默认值是当前时间     createAt:{type:Date,default:Date.now},    // 类型是    user:{type:ObjectId,ref:'User'}//这里是主键和外键的类型一样 ref:'User' 去这个里面找自己需要的id })exports.Article=mongoose.model('Article',ArticaleSchema);